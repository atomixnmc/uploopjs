{
  "name": "uploopjs",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "npm run build -ws --if-present",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  },
  "files": [
    "dist"
  ]
}
